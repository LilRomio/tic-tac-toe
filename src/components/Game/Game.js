'use client';

import React from 'react';
import GameBoard from '../GameBoard/GameBoard';

const Game = () => {
  return (
    <div className="container">
      <GameBoard />
    </div>
  );
};

export default Game;
